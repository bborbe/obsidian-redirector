apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: obsidian-redirector
  namespace: '{{ "NAMESPACE" | env }}'
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: 'traefik'
  rules:
    - host: 'obsidian-redirector.{{ "NAMESPACE" | env }}.quant.benjamin-borbe.de'
      http:
        paths:
          - backend:
              service:
                name: obsidian-redirector
                port:
                  name: http
            path: /
            pathType: Prefix
  tls:
    - secretName: tls-quant
      hosts:
        - 'obsidian-redirector.{{ "NAMESPACE" | env }}.quant.benjamin-borbe.de'
